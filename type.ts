export type MoviesType = {
  movies: {
    movie: {
      title: string;
      description: string;
      imageUrl: string
    };
  }[];
};

export type MovieType = {
  movie: {
    title: string;
    description: string;
    imageUrl: string
  };
};
