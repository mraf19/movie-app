export type MoviesType = {
  movies: {
    movie: {
      title: string;
      description: string;
    };
  }[];
};

export type MovieType = {
  movie: {
    title: string;
    description: string;
  };
};
